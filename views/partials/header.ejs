<!DOCTYPE html>
<html lang="<%= currentLanguage %>">
<head>
    <link href="/stylesheets/style.css" rel="stylesheet">
    <meta charset="UTF-8">
    <title><%= __('header.home') %> - Biblioteka</title>
</head>
<body>
<header>
    <nav class="navigation wrapper">
        <img src="/assets/logo.png" alt="<%= __('header.logo_alt') %>" width="230">
        <ul>
            <li><a href="/" class="<%= navLocation === 'main' ? 'active' : '' %>"><%= __('header.home') %></a></li>
            <li><a href="/books" class="<%= navLocation === 'books' ? 'active' : '' %>"><%= __('header.books') %></a></li>
            <li><a href="/borrowings" class="<%= navLocation === 'borrowings' ? 'active' : '' %>"><%= __('header.borrowings') %></a></li>
            <li><a href="/readers" class="<%= navLocation === 'readers' ? 'active' : '' %>"><%= __('header.readers') %></a></li>
        </ul>
        <div class="language-switcher">
            <form method="get" action="<%= originalUrl %>">
                <button type="submit" name="lang" value="pl" class="<%= currentLanguage === 'pl' ? 'active' : '' %>">PL</button>
                <button type="submit" name="lang" value="en" class="<%= currentLanguage === 'en' ? 'active' : '' %>">EN</button>
            </form>
        </div>
    </nav>
</header>